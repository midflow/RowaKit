/**
 * Version information shown in the demo header.
 *
 * Use the library version so the demo always reflects the current table build.
 */

import { VERSION as TABLE_VERSION } from '@rowakit/table';

export const APP_VERSION = TABLE_VERSION;
